# Web Data Pipeline

Pipeline modulaire de crawling, scraping et stockage des données.

## Structure


## Installation
```bash
python -m venv project_env
source project_env/bin/activate  # (ou .\project_env\Scripts\activate sous Windows)
pip install -r requirements.txt
